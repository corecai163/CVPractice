function [Gmap] = Prewit(Image,threshold)
%%Prewit edge detector
%%0. Smooth the Image
%%1. Compute X and Y derivatives
%%2. Find gradient magnitude
%%3. Threshold gradient magnitude
H = fspecial('average',3);
I = imfilter(Image,H,'conv','symmetric');
%%step0 step1
[gx,gy] = gradient(I);


